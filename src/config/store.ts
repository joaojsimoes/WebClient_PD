import React from 'react';
import { createStore } from 'redux';
/*
interface IStore {
    newsReducer: newsReducer;
}
const getStore = () => {
    
    const reducers: IStore = {
        newsReducer: reducers
    }

    const store = createStore(reducers);
    return store;
}

export default getStore();*/